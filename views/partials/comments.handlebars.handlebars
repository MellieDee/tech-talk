<div class="comments">

  {{#each this}}
    <section class="comment">

      <div class="meta">
        {{user.username}}
        on
        {{shorten_date created_at}}
      </div>

      <div>
        {{comment_text}}
      </div>
    </section>
  {{/each}}

</div>